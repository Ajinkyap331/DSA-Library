#include "lib_dsa.h"
#include <stdio.h>
#include <stdlib.h>

int main()
{
    // QUEUE DONE

    // enqueue(1);
    // enqueue(2);
    // enqueue(3);
    // dequeue();
    // displayQueue();

    // STACK DONE

    // push(1);
    // push(2);
    // push(3);
    // pop();
    // displayStack();

    insertAtFirstLinkedlist(1);
    insertAtFirstLinkedlist(2);
    insertAtFirstLinkedlist(3);
    displayLinkedlist();

    return 0;
}