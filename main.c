#include "lib_dsa.h"
#include <stdio.h>
#include <stdlib.h>

int main()
{   
   
    enqueue(1);
    enqueue(2);
    enqueue(3);
    dequeue();
    displayQueue();
    // insertAtIndexLinkedlist();
   
     
    return 0;
}